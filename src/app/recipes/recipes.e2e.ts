describe('App', () => {

  beforeEach(() => {
    browser.get('http://127.0.0.1:8080/src/');
  });


  it('should have a title', () => {
  
  });

  it('should have <header>', () => {
 
  });

  it('should have <main>', () => {
  });

  it('should have <footer>', () => {
 
  });

});